En Tauri puede que tengas 0 Docker, pero todavía puedes usar Docker para:

Entorno de build reproducible.

Tests automatizados.

En este archivo, o bien:

Explicas que no se usa Docker y por qué.

O documentas una imagen tipo:

- Dockerfile para compilar binarios de producción
- Usa imagen base con Rust + Node
- Genera artefactos que luego se empaquetan
